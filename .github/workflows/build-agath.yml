name: Build Agath

on:
    workflow_dispatch:
        inputs:
            android_application_id:
                description: Android application ID to inject before prebuild.
                required: true
                default: com.rebelrobotics.scoutingapp2024
                type: string
            ios_bundle_identifier:
                description: iOS bundle identifier to inject before prebuild.
                required: true
                default: com.reidf.2638scout
                type: string

permissions:
    contents: read

concurrency:
    group: build-agath-${{ github.ref }}
    cancel-in-progress: true

env:
    CI: '1'
    EXPO_NO_TELEMETRY: '1'

jobs:
    validate:
        name: Validate config
        runs-on: ubuntu-24.04
        timeout-minutes: 20
        steps:
            - name: Check out repository
              uses: actions/checkout@v6

            - name: Set up Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: '24'
                  cache: npm

            - name: Install dependencies
              run: npm ci --prefer-offline --no-audit

            - name: Type-check app
              run: npx tsc --noEmit

            - name: Validate Expo config
              run: npx expo config --type public >/dev/null

    build-android:
        name: Build Android
        runs-on: ubuntu-24.04
        needs: validate
        timeout-minutes: 45
        env:
            ANDROID_APPLICATION_ID: ${{ inputs.android_application_id }}
            EXPO_PUBLIC_APPWRITE_ENDPOINT: ${{ secrets.APPWRITE_ENDPOINT }}
            EXPO_PUBLIC_APPWRITE_PROJECT_ID: ${{ secrets.APPWRITE_PROJECT_ID }}
            EXPO_PUBLIC_APPWRITE_PLATFORM_ID: ${{ inputs.android_application_id }}
            EXPO_PUBLIC_APPWRITE_DATABASE_ID: ${{ secrets.APPWRITE_DATABASE_ID }}
            EXPO_PUBLIC_APPWRITE_COLLECTION_ASSIGNMENTS_ID: ${{ secrets.APPWRITE_COLLECTION_ASSIGNMENTS_ID }}
            EXPO_PUBLIC_APPWRITE_COLLECTION_SCOUTING_DATA_ID: ${{ secrets.APPWRITE_COLLECTION_SCOUTING_DATA_ID }}
            EXPO_PUBLIC_APPWRITE_COLLECTION_PIT_SCOUTING_ID: ${{ secrets.APPWRITE_COLLECTION_PIT_SCOUTING_ID }}
            EXPO_PUBLIC_APPWRITE_FUNCTION_VALIDATE_KEY_ID: ${{ secrets.APPWRITE_FUNCTION_VALIDATE_KEY_ID }}
            EXPO_PUBLIC_APPWRITE_FUNCTION_SUBMIT_SCOUTING_ID: ${{ secrets.APPWRITE_FUNCTION_SUBMIT_SCOUTING_ID }}
        steps:
            - name: Check out repository
              uses: actions/checkout@v6

            - name: Set up Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: '24'
                  cache: npm

            - name: Set up Java
              uses: actions/setup-java@v5
              with:
                  distribution: temurin
                  java-version: '17'

            - name: Set up Gradle
              uses: gradle/actions/setup-gradle@v5

            - name: Configure Android SDK
              shell: bash
              run: |
                  SDK_ROOT="${ANDROID_SDK_ROOT:-${ANDROID_HOME:-/usr/local/lib/android/sdk}}"
                  if [[ ! -d "$SDK_ROOT" ]]; then
                      echo "Android SDK not found at $SDK_ROOT" >&2
                      exit 1
                  fi

                  SDKMANAGER_PATH="$(find "$SDK_ROOT/cmdline-tools" -path '*/bin/sdkmanager' -print -quit 2>/dev/null)"
                  if [[ -z "$SDKMANAGER_PATH" ]]; then
                      echo "sdkmanager not found under $SDK_ROOT/cmdline-tools" >&2
                      exit 1
                  fi

                  echo "ANDROID_HOME=$SDK_ROOT" >> "$GITHUB_ENV"
                  echo "ANDROID_SDK_ROOT=$SDK_ROOT" >> "$GITHUB_ENV"
                  echo "$(dirname "$SDKMANAGER_PATH")" >> "$GITHUB_PATH"
                  echo "$SDK_ROOT/platform-tools" >> "$GITHUB_PATH"

                  set +o pipefail
                  yes | "$SDKMANAGER_PATH" --licenses >/dev/null
                  set -o pipefail
                  "$SDKMANAGER_PATH" --install "platform-tools" >/dev/null

            - name: Install dependencies
              run: npm ci --prefer-offline --no-audit

            - name: Inject Android application ID
              run: |
                  node <<'EOF'
                  const fs = require('fs');
                  const path = 'app.json';
                  const app = JSON.parse(fs.readFileSync(path, 'utf8'));

                  app.expo = app.expo ?? {};
                  app.expo.android = {
                      ...(app.expo.android ?? {}),
                      package: process.env.ANDROID_APPLICATION_ID,
                  };

                  fs.writeFileSync(path, `${JSON.stringify(app, null, 4)}\n`);
                  EOF

            - name: Prebuild Android project
              run: npx expo prebuild --platform android --no-install --non-interactive

            - name: Remove Android release signing
              run: |
                  python <<'EOF'
                  from pathlib import Path

                  path = Path("android/app/build.gradle")
                  lines = path.read_text().splitlines()
                  output = []
                  in_release = False
                  release_depth = 0

                  for line in lines:
                      stripped = line.strip()

                      if stripped == "release {":
                          in_release = True
                          release_depth = line.count("{") - line.count("}")
                          output.append(line)
                          continue

                      if in_release:
                          release_depth += line.count("{") - line.count("}")
                          if stripped.startswith("signingConfig"):
                              continue
                          if release_depth == 0:
                              in_release = False

                      output.append(line)

                  path.write_text("\n".join(output) + "\n")
                  EOF

            - name: Build unsigned Android APK
              run: |
                  cd android
                  chmod +x gradlew
                  ./gradlew --no-daemon --parallel --build-cache assembleRelease
                  cp app/build/outputs/apk/release/app-release-unsigned.apk ../agath.apk

            - name: Upload Android artifact
              uses: actions/upload-artifact@v7
              with:
                  name: agath.apk
                  path: agath.apk
                  compression-level: 0
                  if-no-files-found: error
                  retention-days: 14

            - name: Summarize Android artifact
              run: |
                  {
                      echo "### Android artifact"
                      echo "- \`agath.apk\`"
                  } >> "$GITHUB_STEP_SUMMARY"

    build-ios:
        name: Build iOS
        runs-on: macos-15
        needs: validate
        timeout-minutes: 60
        env:
            IOS_BUNDLE_IDENTIFIER: ${{ inputs.ios_bundle_identifier }}
            EXPO_PUBLIC_APPWRITE_ENDPOINT: ${{ secrets.APPWRITE_ENDPOINT }}
            EXPO_PUBLIC_APPWRITE_PROJECT_ID: ${{ secrets.APPWRITE_PROJECT_ID }}
            EXPO_PUBLIC_APPWRITE_PLATFORM_ID: ${{ inputs.ios_bundle_identifier }}
            EXPO_PUBLIC_APPWRITE_DATABASE_ID: ${{ secrets.APPWRITE_DATABASE_ID }}
            EXPO_PUBLIC_APPWRITE_COLLECTION_ASSIGNMENTS_ID: ${{ secrets.APPWRITE_COLLECTION_ASSIGNMENTS_ID }}
            EXPO_PUBLIC_APPWRITE_COLLECTION_SCOUTING_DATA_ID: ${{ secrets.APPWRITE_COLLECTION_SCOUTING_DATA_ID }}
            EXPO_PUBLIC_APPWRITE_COLLECTION_PIT_SCOUTING_ID: ${{ secrets.APPWRITE_COLLECTION_PIT_SCOUTING_ID }}
            EXPO_PUBLIC_APPWRITE_FUNCTION_VALIDATE_KEY_ID: ${{ secrets.APPWRITE_FUNCTION_VALIDATE_KEY_ID }}
            EXPO_PUBLIC_APPWRITE_FUNCTION_SUBMIT_SCOUTING_ID: ${{ secrets.APPWRITE_FUNCTION_SUBMIT_SCOUTING_ID }}
        steps:
            - name: Check out repository
              uses: actions/checkout@v6

            - name: Set up Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: '24'
                  cache: npm

            - name: Install dependencies
              run: npm ci --prefer-offline --no-audit

            - name: Inject iOS bundle identifier
              run: |
                  node <<'EOF'
                  const fs = require('fs');
                  const path = 'app.json';
                  const app = JSON.parse(fs.readFileSync(path, 'utf8'));

                  app.expo = app.expo ?? {};
                  app.expo.ios = {
                      ...(app.expo.ios ?? {}),
                      bundleIdentifier: process.env.IOS_BUNDLE_IDENTIFIER,
                  };

                  fs.writeFileSync(path, `${JSON.stringify(app, null, 4)}\n`);
                  EOF

            - name: Prebuild iOS project
              run: npx expo prebuild --platform ios --no-install --non-interactive

            - name: Install CocoaPods
              run: pod install --project-directory=ios

            - name: Build unsigned iOS app
              run: |
                  PROJECT_NAME="$(basename "$(find ios -maxdepth 1 -name '*.xcodeproj' -print -quit)" .xcodeproj)"
                  test -n "$PROJECT_NAME"

                  xcodebuild \
                      -workspace "ios/${PROJECT_NAME}.xcworkspace" \
                      -scheme "$PROJECT_NAME" \
                      -configuration Release \
                      -sdk iphoneos \
                      -destination 'generic/platform=iOS' \
                      -derivedDataPath ios/build \
                      COMPILER_INDEX_STORE_ENABLE=NO \
                      CODE_SIGNING_ALLOWED=NO \
                      CODE_SIGNING_REQUIRED=NO \
                      CODE_SIGN_IDENTITY="" \
                      clean build

            - name: Package unsigned iOS IPA
              run: |
                  APP_PATH="$(find ios/build/Build/Products/Release-iphoneos -maxdepth 1 -name '*.app' -type d -print -quit)"
                  test -n "$APP_PATH"

                  rm -rf Payload
                  mkdir -p Payload
                  cp -R "$APP_PATH" Payload/
                  zip -qry agath.ipa Payload
                  rm -rf Payload

            - name: Upload iOS artifact
              uses: actions/upload-artifact@v7
              with:
                  name: agath.ipa
                  path: agath.ipa
                  compression-level: 0
                  if-no-files-found: error
                  retention-days: 14

            - name: Summarize iOS artifact
              run: |
                  {
                      echo "### iOS artifact"
                      echo "- \`agath.ipa\`"
                  } >> "$GITHUB_STEP_SUMMARY"
